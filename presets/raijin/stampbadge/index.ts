const preset = {
  root: {
    className:
      'inline-block border-2 rounded px-2 py-0.5 text-[0.6rem] font-black uppercase tracking-[0.15em] opacity-90 select-none',
  },
};

export default preset;
