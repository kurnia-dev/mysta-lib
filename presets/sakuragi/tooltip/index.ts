import { TooltipPresetOptions } from 'lib/components/tooltip/Tooltip.d';

const preset: TooltipPresetOptions = {
  content: {
    className: [
      'px-2 py-1 bg-secondary-500 text-white text-xs rounded-md max-w-xs',
    ],
  },
};

export default preset;
